cmake_minimum_required(VERSION @CMAKE_MINIMUM_REQUIRED_VERSION@)
project(check_language_@language@)
enable_language(@language@)
if (CMAKE_@language@_COMPILER)
  file(WRITE @fcompiler@ ${CMAKE_@language@_COMPILER})
endif()
