message(DEBUG "EXTERNAL BUILD")
set(PREPEND_PATH "@ARGS_PREPEND_PATH@")
if(PREPEND_PATH)
  if(WIN32)
    set(ENV{PATH} "${PREPEND_PATH}\;$ENV{PATH}")
  else()
    set(ENV{PATH} "${PREPEND_PATH}\:$ENV{PATH}")
  endif()
endif()
execute_process(COMMAND "@CMAKE_COMMAND@" --build . )
