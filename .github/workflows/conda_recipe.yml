name: Test of conda recipe build
'on':
  push:
    branches-ignore:
     - gh-pages
    tags:
     - '*'
  schedule:
  - cron: 0 10 * * 1
jobs:
  build_conda_recipe:
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -el {0}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-2022]
        python-version: ["3.11"]
        builder: [rattler]
        include:
          - os: windows-2022
            python-version: "3.11"
            builder: rattler-m2w64
          - os: windows-2022
            python-version: "3.11"
            builder: rattler-unsplit
      fail-fast: false
    steps:
      - name: Check out repository code
        uses: actions/checkout@v5
        with:
          submodules: true

      ###################################
      # CONDA SETUP
      ###################################
      - name: Set up MSVC Compiler on windows
        uses: ilammy/msvc-dev-cmd@v1
        if: matrix.os == 'windows-2022'
      - name: Set up miniconda test environment
        uses: conda-incubator/setup-miniconda@v4
        with:
          activate-environment: ""
          auto-activate: true
          auto-update-conda: true
          channels: conda-forge
          channel-priority: strict
          miniforge-version: latest
          conda-remove-defaults: true
      - name: Install rattler-build
        # if: matrix.builder == 'rattler-unsplit'
        run: |
          conda info
          conda list
          conda install rattler-build -y
      # - name: Install rattler-build
      #   if: matrix.builder != 'rattler-unsplit'
      #   run: |
      #     conda info
      #     conda list
      #     conda install "rattler-build<0.66" -y
      - name: Check conda installation
        run: |
          conda info
          conda list
      - name: Build conda recipe (using rattler-build)
        if: matrix.builder == 'rattler'
        run: |
          rattler-build build --recipe "conda.recipe/recipe.yaml" --output-dir $HOME/local_channel/ --variant python=${{ matrix.python-version }} --no-build-id --no-include-recipe
      - name: Build conda recipe (using rattler-build)
        if: matrix.builder == 'rattler-m2w64'
        run: |
          rattler-build build --recipe "conda.recipe/recipe.yaml" --output-dir $HOME/local_channel/ --variant python=${{ matrix.python-version }} --variant toolchain_variant=m2w64 --no-build-id --no-include-recipe
      - name: Build conda recipe (using rattler-build)
        if: matrix.builder == 'rattler-unsplit'
        run: |
          rattler-build build --recipe "conda.recipe/recipe-unsplit.yaml" --output-dir $HOME/local_channel/ --variant python=${{ matrix.python-version }} --no-build-id --no-include-recipe
      # - name: Build conda recipe (using rattler-build action)
      #   uses: prefix-dev/rattler-build-action@v0.2.34
      #   if: matrix.builder == 'rattler'
      #   with:
      #     upload-artifact: false
      #     build-args: --output-dir $HOME/local_channel/ --variant python=${{ matrix.python-version }} --no-build-id --no-include-recipe


  all_recipes_built:
    runs-on: ubuntu-latest
    name: All recipes were successfully built
    needs: [build_conda_recipe]
    if: always()
    steps:
      - name: All tests ok
        if: ${{ !(contains(needs.*.result, 'failure')) }}
        run: exit 0
      - name: Some tests failed
        if: ${{ contains(needs.*.result, 'failure') }}
        run: exit 1
